{
  "name": "@projeto-sete/api-src",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "echo 'built by root build:api'",
    "dev": "tsx watch src/server.ts",
    "typecheck": "tsc --noEmit"
  }
}
